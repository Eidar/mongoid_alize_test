<%= form_with(model: valute, local: true) do |form| %>
  <% if valute.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(valute.errors.count, "error") %> prohibited this valute from being saved:</h2>

      <ul>
      <% valute.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
